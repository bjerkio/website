import React from 'react'
import PropTypes from 'prop-types'

const Blog = () => (
    <div>
      Blog
    </div>
  )
  
  Blog.propTypes = {
    data: PropTypes.object.isRequired,
  }
  
  export default Blog